# Session handoff — 2026-07-21 (written for the next session to take off from)

One-line state: **all known bugs fixed or verified, the extension seam is
designed and locked (PLAN §16), and v0.2.0 is ready to cut.** The 12:16
verification run was green end to end (worst stall 153ms across a full
edit/drag/delete session).

## What landed today (commit order)

| commit | what |
|---|---|
| `e5b572e` | Register: portal doc-IDs are **reused** (measured — no index leak, V-19 deferral safe); portal trash **cannot** reach the real file (measured); V-04 caught live (2-file scan queued ~5 min behind launch rescan); V-22 opened |
| `6201817` | **Find Duplicates scoped to the open folder** (`under` threaded through `exact/near_duplicates`, header subtitle names the scope) + **V-22 fixed**: both trash paths toast on *results*; `mark_missing` only on success |
| `215bb79` | New Pages article `docs/duplicates-that-arent.html` ("The duplicate that was one file") — the whole portal/dedup arc, cross-linked from the other two articles |
| `2277453` | **Viewer Folder row navigates**: friendly path (`scope_display` — `~`-relative, doc prefix stripped; raw in tooltip), row click browses in grid (`win.browse-folder`), suffix button opens Nautilus (`gtk::FileLauncher`, portal-safe) |
| `dee77c7` | **PLAN §16 — extension seam decided** (see below) |
| `c4d118d` | ffmpeg's first helper recipe noted: frame-capture from videos → reference stills |
| `aeb6f6a` | **V-04 fixed**: user-priority scan queue + in-scan checkpoints (64-file stride); probe `VDBG-SCANYIELD`; eb0a701 VERIFIED (535ms worst vs 2.3s pre-fix); V-21 third attempt (conservative `alpha(@accent_bg_color)` syntax — measured solid slab from screenshot pixels, `color-mix(var())` silently failing was the suspect); **trap recorded: `debug-run.sh` runs the INSTALLED flatpak, never the tree** |
| `a1707a3` | 12:16 run recorded: green. V-04 write-half verified (writes `queued≤1` mid-rescan), V-21/V-22/Folder-row/delete verified in use |

## Verified vs still-open (full detail: ISSUES.md, bottom sections)

**Verified in use today:** V-04 (write half), V-21 (selection wash finally
renders), V-22 (delete honest), Folder row both exits, dedup scoping,
eb0a701 (twice), doc-ID reuse, portal-trash-refusal.

**Open, all minor:**
1. V-04 *nested* preemption path never exercised (open a never-indexed folder
   in the first ~2 min after launch; expect a `VDBG-SCANYIELD` line + summary
   count ≥ 1). Note the probe undercounts by design — front-of-queue service
   between root rescans is silent.
2. V-22 error-toast wording never actually seen (portal sacrifice re-test).
3. V-02's honest limit stands: "accepted ≠ committed" (reply channel from the
   worker deliberately not built).
4. Find Duplicates is still labeled Experimental; V-19 `host_path` structural
   fix stays comfortably deferred (doc-IDs reused — measured).

## Decisions locked today (don't relitigate, PLAN §16)

- **Lua via `mlua`** for scripting (Rhai dropped); **WASM/wasmtime deferred**
  until auto-tagging/embeddings is committed; **helpers as Flatpak
  `add-extensions`** (`…Vitrine.Helper.magick` first, ffmpeg later — first
  ffmpeg recipe: frame capture → reference stills, user's existing workflow).
- Host API: versioned `vitrine` table, subtractive sandbox, **no raw SQL ever**,
  writes via Annotator, providers register / host executes.
- The Magick window (§16.4): Lua recipes → sliders, proxy preview, edit-tier
  commit boundary (preview-only until explicit Apply + destination policy).
- Phases E0 (done — §16 itself) → E1 Lua host + custom sorts (acceptance:
  natural sort `img_2 < img_10`, no stall regression, script errors toast) →
  E2 helper point + batch runner → E3 Magick window → E4 WASM.

## The agreed queue (in order, flexible)

1. **Cut v0.2.0.** Changelog is essentially the commit table above plus the
   2026-07-20 fixes. Release checklist lives in the v0.1.0 notes
   (memory: `vitrine-v0-1-0-release`); rebuild manifest
   `build-aux/io.github.superuser_miguel.Vitrine.release.yml`.
2. **E1 — Lua host + custom sort orders** (PLAN §16.6; §10.3.1 has the design).
3. E2 helper extension point → E3 Magick window.
4. Core items that stay core: global search V-11 (FTS5 when needed),
   Date Taken sort V-12 (enrichment-callback groundwork documented).
5. Parallel track when asked: **Video Gallery app PLAN doc** (memory:
   `video-gallery-app-vision` — separate app, decided; do not build unprompted).

## Traps for the next session (cost us time today)

- **`debug-run.sh` runs the installed flatpak.** Rebuild + reinstall before
  any verification run: `flatpak-builder --user --install --force-clean
  build-dir build-aux/io.github.superuser_miguel.Vitrine.yml`
- GTK CSS `color-mix(var(--…))` can fail **silently at compute time** (no
  parse warning). Use `alpha(@named_color)`; if styling misbehaves, pixel-check
  a screenshot before theorizing, and `GTK_DEBUG=interactive` before guessing.
- `VDBG` HUD: read `stall`, not `frame_max`; `cache_hit=0%` under `--cold` is
  by design (register V-16 notes).
- Register discipline: touch the ISSUES.md entry in the same commit as the fix.

## Key files

- `Troubleshoot/ISSUES.md` — the register; today's sections at the bottom.
- `PLAN.md` §16 — extension seam; §13 perf; §15 portal split.
- `docs/` — public site + three articles (portal, six-bugs, duplicates).
- `crates/vitrine-app/src/index.rs` — the new worker (queue + checkpoints).
- `build-aux/debug-run.sh` — HUD runner (`--interact` for probe-only logs).
